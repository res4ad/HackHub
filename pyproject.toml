[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hackhub"
version = "1.0.0"
description = "Operator-focused multi-session listener"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
hackhub = "hackhub.cli:run"